<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shengsu.mapper.JPushMapper">
    <insert id="save" parameterType="JpushScheduleCancel">
        INSERT INTO jpush_schedual_record
            (
                message_id,
                schedule_id,
                create_time,
                modify_time,
                del_flag
            )
            VALUES
            (
                #{messageId},
                #{scheduleId},
                current_timestamp(),
                current_timestamp(),
                0
            )
    </insert>
    <select id="list" parameterType="JpushScheduleCancel" resultType="JpushScheduleCancel">
        SELECT
        schedule_id
        FROM
        jpush_schedual_record
        WHERE
        message_id=#{messageId}
    </select>
</mapper>